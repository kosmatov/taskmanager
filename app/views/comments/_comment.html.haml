%li
  %div
    %a{:href => user_path(comment.user)}
      = gravatar_for comment.user, :size => 32, :class => :gravatar
      %strong= comment.user.name
    %small
      = time_ago_in_words(comment.created_at)
    %p= comment.content
